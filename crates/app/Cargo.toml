[package]
name = "fluxdown_ui_app"
version = "0.1.0"
edition.workspace = true

[[bin]]
name = "fluxdown-desktop"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
fluxdown_ui_shell = { path = "../shell" }

[target.'cfg(windows)'.build-dependencies]
winresource.workspace = true
